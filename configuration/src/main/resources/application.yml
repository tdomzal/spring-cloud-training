server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/landrzejewski/spring-cloud-training
          search-paths: configuration-files
        encrypt:
          enabled: false
